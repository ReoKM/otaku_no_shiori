# オタクのしおり — プロジェクト共通ルール

このファイルは全エージェント・全セッションが従う共通ルール。
各エージェントの個別定義は `.claude/agents/` にある。

## プロジェクト概要

オタクの遠征(ライブ・イベント・聖地巡礼)用しおり作成サービス。
オーナーはコードを書かない。開発・運用・集客はAIエージェントチームが行う。

## 仕様の正(Source of Truth)

仕様の判断はすべて `docs/` を根拠にする。記憶や推測で判断しない。

| ファイル | 内容 |
|----------|------|
| `docs/00_vision.md` | ビジョン・ターゲット・北極星指標 |
| `docs/01_service_spec.md` | MVP機能(F1〜F10)・画面(S1〜S6)・非スコープ |
| `docs/02_roadmap.md` | フェーズ計画・週次マイルストーン・KPI |
| `docs/03_tech_stack.md` | 技術構成・データモデル・RLS・コスト |
| `docs/04_growth_monetization.md` | 集客・SNS運用・収益化 |
| `docs/05_agent_team.md` | エージェント体制・ライティング規約 |

仕様と実装がズレたら、勝手にどちらかへ合わせない。`/spec-sync` を使って差分を報告する。

## 全員が守るルール

1. 出力・コミットメッセージ・ドキュメントは日本語で書く
2. `main` に直接pushしない。すべてPR経由。PRは小さく(目安: 1機能1PR、変更400行以内)
3. 仕様(`docs/`)にない機能を勝手に追加しない。追加したい場合はPM経由でオーナーに提案する
4. 秘密情報(APIキー・service_roleキー)をコード・ログ・ドキュメントに書かない
5. 変更したらCI(lint + `tsc --noEmit` + テスト)が通ることを確認してからPRを出す

## タスク実行プロトコル(全員・全タスク必須)

すべてのタスク(スキル実行を含む)は次の4ステップで実行する。省略禁止。

### 1. 開始: Goalを立てる

- 作業を始める前に、このタスクのGoalを1行で書く
- 例: 「Goal: S2画面がゲスト保存で動き、PRがマージ可能な状態になる」
- Goalが決められない・前提が不明な場合は着手しない。不明点を報告して指示を待つ

### 2. 途中: 不明点は止まって報告

- 仕様の曖昧さ・判断できない選択肢が出たら、推測で進めず報告する
- 軽微な仮置きで進めた場合は、仮置きした内容を完了報告に必ず書く

### 3. 終了: 完了報告(フォーマット固定)

タスクの最終出力(PR説明・Issueコメント・呼び出し元への返答)に、必ず次の形式の完了報告を含める:

```markdown
## 完了報告
- Goal: (開始時のGoalを再掲)
- 結果: 達成 / 部分達成 / 未達
- できていないこと: (無い場合も「なし」と明記。あれば箇条書き)
- 不明点・仮置き: (無い場合も「なし」と明記。あれば箇条書き)
- 成果物: (PR番号・ファイルパス)
- 作業ログ: docs/logs/work/YYYY-MM-DD_<エージェント名>.md
```

ルール:

- 「できていないこと」「不明点・仮置き」の行を省略した報告は**不完全**。レビュー・受け入れで差し戻す
- 結果が「達成」でも、テスト未実行・確認省略があれば「できていないこと」に書く。隠さない

### 4. ログ: 作業ログを残す

- `docs/logs/work/YYYY-MM-DD_<エージェント名>.md` に追記する(1日1ファイル、同日2タスク目からは追記)
- 内容: 完了報告と同じ6項目+「やったこと」の箇条書き
- テンプレートは `docs/logs/work/README.md` にある

## 無料枠ガードレール(コストを壊さない)

- Supabase Storageに保存する画像は必ずクライアントで長辺1600pxにリサイズ済みであること
- Storageバケットの Max File Size(2MB)/許可MIMEタイプ制限を外さない
- 写真上限(1しおり20枚)は環境変数でのみ変更可。ユーザー設定にしない
- N+1クエリ・無限ループするAPI呼び出しを書かない
- Netlifyの本番デプロイはまとめる。mainマージごとに自動本番公開しない(無料枠は月300クレジット・本番デプロイ15クレジット/回。詳細は `docs/03_tech_stack.md`)。PR確認はプレビューデプロイで行う
- 有料プラン・有料APIの導入はオーナー承認が必要

## セキュリティ最低ライン

- 全テーブルにRLSを設定する。子テーブルは親 `shiori.user_id` を参照する `EXISTS` ポリシー必須
- ユーザー入力は必ず検証する(長さ・型・URLエンコード)
- 外部に出すURLの `query` パラメータは `encodeURIComponent` する

## オーナーへのエスカレーション(独断禁止)

以下は必ずオーナーの判断を仰ぐ:

- お金(有料プラン・課金設計・広告配置の大幅変更)
- 対外(SNS投稿の実行・外部団体への連絡・キャンペーン開始)
- 権利・法務(画像・名称の利用・規約変更)
- ロードマップ変更(フェーズ入替・機能の大幅追加削除)

## ライティング規約(エージェント定義・スキル・ドキュメント)

`.claude/` 配下のファイルを書く・直すときは `docs/05_agent_team.md` の規約に従う:

1. 短い文で書く。1文1指示
2. 手順は番号付きリスト。「適切に」「いい感じに」を使わない
3. 入出力例を1つ以上載せる
4. やらないことを明記する
5. 判断基準は表かチェックリストにする
6. 参照先は `docs/` のファイル名で固定する
7. 出力フォーマット(見出し・パス・命名)を固定する
8. 冒頭に「役割1行」「成果物」「完了条件」を書く
