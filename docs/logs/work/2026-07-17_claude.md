## 11:00 デザイン全面刷新(DESIGN.md準拠のパープル基調リブランド)

- Goal: 共有された`DESIGN.md`(パープル基調・Space Grotesk)のデザイン仕様を、全画面に反映したPRがマージ可能な状態になる
- 結果: 達成
- やったこと:
  - `docs/design/tokens.md`をDESIGN.mdの値で更新(カラー・角丸・タイポグラフィ・新規のシャドウ/インタラクション状態セクション追加)
  - `src/app/globals.css`の`@theme`でTailwindデフォルトパレット(pink→パープル、neutral→暖色系、red/amberの実使用シェード)と`radius-*`/`shadow-*`の値を上書き。コンポーネント側のクラス名(`bg-pink-500`等)は変更せず、全ページへ一括反映する方式を採用
  - フォントをGeist(見出し)+システムフォント(本文)からSpace Grotesk単一ファミリーへ変更(`src/app/layout.tsx`)。あわせて`globals.css`のbodyフォント指定バグ(Arial固定でGeist/Tailwind指定が効いていなかった)を修正
  - キーボード操作時のフォーカスリング(2px実線+2pxオフセット、primary色)をグローバルCSSで追加
  - ブランドカラーを使う周辺ファイルを更新: `public/manifest.webmanifest`・`public/icon.svg`(theme_color)、`src/templates/share-image/colors.ts`(共有画像のCOLORS定数)
  - `lint` / `typecheck` / `test`(317件)/ `build`が全て成功することを確認
  - Playwright(Chromium)でホーム・しおり作成フォーム・利用規約・TODOタブの404状態の4画面を375px幅相当でスクリーンショット確認し、パープル/暖色ニュートラル/Space Grotesk(数字部分)が意図通り反映されていることを目視確認
- できていないこと:
  - `hover`/`active`のインタラクション状態(明度±5%/10%)は未適用。既存コードにボタンの`hover:`クラスが元々無く、45件超のボタン全てへの追加は今回のトークン刷新のスコープを超えるため見送った(`docs/design/tokens.md`「8. インタラクション状態」に明記)
  - ダークモードは未対応。DESIGN.mdはダークモード対応と謳っているが、既存コードは`dark:`クラスを1件も使っておらず、全コンポーネントへの追加は本PRのスコープを大きく超えるため見送った
  - `success`/`info`のブランド色はDESIGN.mdにあるが現行コードで使う箇所が無いため`globals.css`に未定義(tokens.md「1-5」に将来用として記載)
  - 共有画像(satori)側のフォントはNoto Sans JP(CJK対応のため)のまま維持し、Space Grotesk化はしていない(そもそも別レンダリングパイプラインで、日本語グリフが必須のため対象外と判断)
- 不明点・仮置き:
  - 角丸トークンは、既存コードがボタン/入力欄/カードで同じTailwindクラス(`rounded-lg`等)を使い回している箇所があり、DESIGN.mdのコンポーネント別ロール(button:4px/input:2px/card:6px)を完全に個別再現できないため、`rounded-sm/md/lg/xl`をDESIGN.mdの数値にそのまま1段階シフトする形で近似した(tokens.md「3. 角丸トークン」に明記)
  - `red-*`/`amber-*`はDESIGN.mdが単色(danger/warning)しか定義していないため、実使用シェード(50/400/600、50/400/700)のみHSL補間で自前生成した(tokens.md「1-3」「1-4」)
  - カバー(しおり)プリセット8色・遠征タイプ別アクセントカラー(共有画像)はブランド色と独立した装飾色のため、意図的に変更していない
- 成果物: PR(本ログ作成時点で未作成、この後push・PR作成予定)
