# 2026-07-25 frontend-dev

## 17:20 UIリニューアル(1/5) デザイントークン+ヘッダー/タブバー
- Goal: 紙テイストのデザイントークンが `globals.css` に入り、S3のヘッダーとタブバーが新デザインで表示され、CI(lint + tsc + テスト)が通る
- 結果: 達成
- やったこと:
  - `src/app/globals.css` を全面書き換え。paper(面)/ink(文字)/sakura(差し色)の3系統・22トークンを `@theme` に定義。Geist変数とダークテーマ定義を撤去
  - アニメーション3種(`paper-flash` / `paper-sheet-up` / `paper-fade-up`)と `prefers-reduced-motion` の抑制指定を追加
  - 本文フォントを Zen Kaku Gothic New(next/font、weight 400/500/700/900)に変更。`subsets` は指定しない方針を採用
  - `theme_color` を `#f6339a` → `#D6146F`、`background_color` を `#ffffff` → `#FDFBF7` に更新(`layout.tsx` と `manifest.webmanifest` を同値で揃えた)
  - `ShioriDetailHeader` を刷新: 22px/900・2行のタイトル、共有導線をテキストリンクからアイコンボタン(44×44px)へ変更、スクロール連動のコンパクト表示に対応
  - `src/lib/use-compact-header.ts` を新規追加(縮め36px/戻し12pxの二段閾値)
  - `TabBar` を刷新: ラベルを「TODO」→「やること」、「ログ」→「記録」に変更。選択中は `sakura-ink` 太字+下辺3pxインクバー。`aria-current="page"` を追加
  - `BackButton` の「‹」文字をSVGアイコンに差し替え
  - S3レイアウトでヘッダーとタブバーを1つの `sticky` ブロックにまとめた
  - `docs/design/tokens.md` のカラー/角丸/タイポグラフィ節を新トークン体系に書き換え、`docs/design/screens/S3_しおり詳細.md` のヘッダー・タブバー仕様を更新
- できていないこと:
  - 持ち物・やること・旅程・記録の各タブ本体は未着手(PR②〜⑤で対応)。このPRのマージ後、各タブは旧トークン(neutral/pink)のまま残るため一時的に見た目が不揃いになる
  - 実機・ブラウザでの表示確認をしていない(この環境で開発サーバを立ち上げての目視確認は未実施)。`next build` の成功と静的検査までは確認済み
  - フォント読み込み後の実測LCP・CLSを計測していない
- 不明点・仮置き:
  - デザイン案の #4A423A を #3B342E(`ink-strong`)に統合した。視認上ほぼ同一でトークン数を抑えるための判断。tokens.md に明記済み
  - ヘッダー右の「その他メニュー」(ケバブ)は、デザインに描かれているが中身の項目が未定義のため実装していない。何を入れるかオーナー判断が必要
  - タブの表示名のみ変更し、URLパス(`/todo`・`/log`)は据え置いた。既存リンクとService Workerのキャッシュルーティングを壊さないため
  - ダークテーマ定義を削除した。紙テイストは明るい面前提の配色で、デザインにダーク版が無いため
  - フォントのweightは4種すべて読み込んだ(ビルド成果物のwoff2は484ファイル/計5.6MB)。`preload: false` のためユーザーが実際に取得するのは必要なunicode-rangeチャンクのみ
- 成果物: `src/app/globals.css`、`src/app/layout.tsx`、`src/app/shiori/[id]/layout.tsx`、`src/components/shiori-detail/ShioriDetailHeader.tsx`、`src/components/shiori-detail/TabBar.tsx`、`src/components/common/BackButton.tsx`、`src/lib/use-compact-header.ts`、`public/manifest.webmanifest`、`docs/design/tokens.md`、`docs/design/screens/S3_しおり詳細.md`

## 17:45 UIリニューアル(2/5) 持ち物タブ
- Goal: 持ち物タブが採用案2aの見た目・操作(準備状況カード/ツールバー/リストカード/インライン追加/並べ替えシート/行メニュー/追従ボタン/取り消しトースト)で動き、CIが通る
- 結果: 達成
- やったこと:
  - `src/lib/list-sort-mode.ts` を新規追加。並び順4モード(登録順/未完了を上に/完了済みを上に/手動)の型・ラベル・安定ソート・localStorage永続化(しおり×タブ単位)。単体テスト12件を追加
  - 共有部品を `src/components/list-ui/` に新設: `BottomSheet` / `SortSheet` / `RowActionSheet` / `UndoToast` / `FollowFab` / `ListCheckbox` / `icons`
  - `src/lib/use-undo-toast.ts`(5秒で自動クローズ)、`src/lib/use-follow-fab.ts`(末尾の追加行が画面外のときだけ表示)を新規追加
  - `PackingProgressCard` をピンクのグラデーションカードからフラットな枠線カードへ差し替え(「n／m完了　残りn件」+10pxバー)
  - `PackingAllDoneBanner` を新規追加(全件チェック済みのときだけ表示)
  - `PackingToolbar` をリストカードの外へ出し、「完了済みを隠す/表示」を追加。「並べ替え」はシートを開く形に変更
  - `PackingRow` を68px行に作り直し。行の左側全体を完了トグルにし、編集・削除は行右端の「…」→`RowActionSheet` に集約
  - `PackingAddForm` を画面下固定からリストカード末尾のインライン追加へ変更。重複警告・Enter連続入力・Escapeで閉じるに対応
  - `EmptyPacking` を破線カードに変更し、主CTAを「＋ 最初の持ち物を追加」、副導線を「テンプレから追加」に整理
  - `PackingRowSortMode` / `PackingListSkeleton` を新トークンで作り直し
  - Playwright(Chromium/390×800)で実機相当の目視確認を実施。通常表示・スクロール時のコンパクトヘッダー・並べ替えシート・行メニュー・全件完了・完了済みを隠す・インライン追加と重複警告・削除と取り消しトースト・手動並べ替え・並び順のリロード復元・空状態の11状態を確認。コンソールエラー/ページエラーなし
  - `docs/design/screens/S3a_持ち物.md` のレイアウト・コンポーネント構造・状態・タップ挙動・文言一覧を全面更新
- できていないこと:
  - やること・旅程・記録タブは未着手(3/5〜5/5で対応)
  - 追加した共有部品(`list-ui/`)とフック2種に単体テストが無い。DOM前提のためテスト環境がnodeのままでは書けず、jsdom導入の可否を判断していない。ロジックのある `list-sort-mode.ts` のみテスト済み
  - `useFollowFab` / `useCompactHeader` のスクロール判定を実機のiOS Safari(アドレスバー伸縮あり)で確認していない
- 不明点・仮置き:
  - 削除の取り消しは「同じ名前で作り直す」実装のため、**元の位置ではなく末尾に戻る**。guest-storeに任意位置への挿入APIが無いための仮置き
  - 「完了済みを隠す」の状態は保存していない(タブを開き直すと表示に戻る)。並び順モードのみ永続化した
  - 同名の持ち物は警告を出すが登録は妨げない仕様にした(同じ物を複数持つ場合があるため)
  - 「手動で並べ替え」を選ぶと「完了済みを隠す」を強制OFFに戻す(隠れた行を巻き込んで並べ替えるのを防ぐため)
  - 空状態でツールバーの「まだ登録がありません」と空カードの「持ち物はまだありません」が二重に出る。デザイン案どおりの構成だが冗長。整理するかは要判断
- 成果物: `src/lib/list-sort-mode.ts`(+テスト)、`src/lib/use-undo-toast.ts`、`src/lib/use-follow-fab.ts`、`src/components/list-ui/`(7ファイル)、`src/components/packing/`(8ファイル)、`docs/design/screens/S3a_持ち物.md`

## 17:55 UIリニューアル(3/5) やることタブ
- Goal: やることタブが採用案2aの見た目・操作で動き、F3「期限が近い順に表示」を落とさず、CIが通る
- 結果: 達成
- やったこと:
  - `list-sort-mode.ts` をタブ別の選択肢構成に拡張。`SORT_MODES_BY_TAB` / `DEFAULT_SORT_MODE_BY_TAB` を追加し、やること限定の並び「期限が近い順」を新設して既定にした
  - `loadListSortMode` を「そのタブで選べないモードが保存されていたら既定値に戻す」挙動に変更。テストを13件へ増やした
  - `SortSheet` を選択肢リスト受け取り式に変更(持ち物4択 / やること5択)
  - `TodoProgressCard` を新規追加(見出し「やること」)
  - `Toolbar` を持ち物と同じ構成に作り直し(件数+完了済みの表示切替+並べ替えシート)
  - `TodoRow` を68px行に作り直し。期限を名前の下に置き、期限当日の強調(琥珀色の背景+左4pxの縦線+「・今日」)を維持
  - `TodoActionSheet` を新規追加(名前+期限の編集、期限を消す、削除の1段階確認)
  - `AddForm` を画面下固定からインライン追加へ変更。追加時の入力を名前1つに絞った
  - `EmptyTodo` / `TodoSkeleton` / `TodoRowSortMode` を新トークンで作り直し
  - 手動ドラッグのグループ制限を「期限が近い順」のときだけ効かせるようにした
  - Playwright(Chromium/390×800)で目視確認。テンプレ投入直後の一覧・行メニューでの期限設定・期限当日の強調・並べ替えシート(5択であること)・完了済みを隠す・インライン追加を確認。コンソールエラー/ページエラーなし
  - `docs/design/screens/S3b_TODO.md` のレイアウト・ソート規則・コンポーネント構造・状態・タップ挙動・文言一覧を全面更新
- できていないこと:
  - 旅程・記録タブは未着手(4/5・5/5で対応)
  - `TodoActionSheet` / `AddForm` などDOM前提の部品に単体テストが無い(2/5と同じ理由。jsdom導入の可否が未判断)
  - 手動並べ替えのドラッグ操作そのものはブラウザで実操作していない(長押し+ポインタ移動の自動化が不安定なため、表示のみ確認)
- 不明点・仮置き:
  - **「期限が近い順」はデザイン案の4択に無い選択肢**。F3「期限が近い順に表示」を落とさないために追加し、やることタブの既定にした。デザイン優先で削るかは要判断
  - 追加フォームから期限入力を外し、期限は登録後に行メニューから設定する形にした(デザイン案のヒント文「期限は登録後に行から設定できます」に合わせた)
  - 削除の取り消しは持ち物と同じく末尾に戻る
  - 全件完了時の達成メッセージは持ち物タブのみとし、やることには置かなかった(相当する声かけ文言が無いため)
- 成果物: `src/lib/list-sort-mode.ts`(+テスト)、`src/components/list-ui/SortSheet.tsx`、`src/components/todo/`(9ファイル)、`docs/design/screens/S3b_TODO.md`

## 18:05 UIリニューアル(4/5) 旅程タブ
- Goal: 旅程タブ(旅程/行きたい場所)が採用案2aの見た目で動き、CIが通る
- 結果: 達成
- やったこと:
  - `SegmentedControl` をピル型から下線型(タブバーと同じ「太字+インクバー」)に変更。インクバーは2.5pxにしてタブバー(3px)との階層差を出した
  - `ItineraryToolbar` を持ち物・やることと同じ見た目に統一。件数を「全n件・m日間」に変更し、`dayCount` を受け取るようにした
  - `DayBlock` を「見出しはカードの外・予定はカードの中」の構造に作り直し、カード末尾に「＋ 予定を追加」行を置いた
  - `EntryRow` の通常表示を刷新(時刻52px固定幅+予定名+ピン付きの場所+メモ+シェブロン)。**行内の編集✎・削除🗑ボタンを廃止し、行全体タップで編集表示を開く形にした**
  - 編集表示に「地図で見る」と「この予定を削除する」を移設。地図リンクは通常表示から消えるため、機能を落とさないようここへ集約した
  - `EntryRow` / `AddEntryForm` の入力欄すべてに見出しラベルを追加(5欄が並ぶためプレースホルダーだけでは判別できない)
  - `SpotRow` / `AddSpotBar` / `FreeSpotForm` / `EmptySpots` / `EmptyDay` / `ItinerarySkeleton` / `ItineraryFallback` / `EntryRowSortMode` を新トークンで作り直し
  - `SpotSection` のリストをカードで包み、件数表示を「全n件」に変更
  - Playwright(Chromium/390×800)で目視確認。空状態・予定2件の一覧・行タップでの編集表示・並べ替えモード・行きたい場所の空状態と1件表示を確認。コンソールエラー/ページエラーなし
  - `docs/design/screens/S3c_旅程スポット.md` のレイアウト・コンポーネント構造・文言差分を更新
- できていないこと:
  - 記録タブは未着手(5/5で対応)
  - **旅程タブには並べ替えシート(4択)・追従ボタン・取り消しトーストを入れていない。** デザイン案の旅程タブにこれらが描かれておらず、日ごとの手動並べ替えという既存の仕組みと噛み合わないため見送った
  - スポット検索画面(S4)・共有画像画面(S5)は旧トークンのまま。デザイン案の対象外
  - 「旅程に追加」直後の強調表示はコード上維持しているが、ブラウザで実操作しての確認はしていない
- 不明点・仮置き:
  - 行タップで編集を開く形にした。デザイン案の行末シェブロンを「詳細/編集を開く」の意味と解釈した。持ち物・やることの「…」方式とは操作が揃っていない
  - デザイン案の日見出しにある副題(「移動＋カフェ巡り」)は、対応するデータを持たないため実装していない
  - 旅程タブのツールバーに「完了済みを隠す」相当の機能は無い(予定に完了状態が無いため)
- 成果物: `src/components/itinerary/`(13ファイル)、`docs/design/screens/S3c_旅程スポット.md`

## 18:12 UIリニューアル(5/5) 記録タブ
- Goal: 記録タブが採用案2aの見た目で動き、共有導線カードが入り、CIが通る
- 結果: 達成
- やったこと:
  - `LogToolbar` を新デザインへ。「削除」→「選択」、「キャンセル」→「選択をやめる」に文言変更。「写真を追加」を白地+桜枠のピル(＋アイコン付き)に変更
  - 削除確認バーをカード化し、文言を他タブと同じ「選択したn枚を削除します。よろしいですか?」に統一
  - `LogDateGroup` に枚数表示を追加し、見出しを旅程タブの日見出しと同じ字送りに揃えた
  - `LogShareCard` を新規追加(写真が1枚以上のときだけリスト下に表示)。デザイン案の「しおりを共有する」カードを実装した
  - `EmptyLog` を破線カードに変更し、主CTAを「＋ 最初の写真を追加」に
  - `LogPhotoCard` / `LogPhotoCardEditing` / `LogPhotoCardProcessing` / `LogPhotoCardError` / `LogSkeleton` / `AdSlotPlaceholder` を新トークンで作り直し
  - 編集カードの保存/キャンセルをアイコン(✓/×)から文言ボタンに変更
  - **既存の不具合を1件修正**: ObjectURL確保前の初回レンダーで `<img src="">` になり、ブラウザがページ全体を再取得しうる警告が出ていた。URLが確定するまで`img`自体を描画しないようにした
  - Playwright(Chromium/390×800)で目視確認。空状態・写真3枚の一覧・共有導線カード・写真タップでの編集・選択モードと削除確認を確認。修正後はコンソールエラー/ページエラーなし
  - `docs/design/screens/S3d_ログ.md` のレイアウト・コンポーネント・文言を更新
- できていないこと:
  - スポット検索画面(S4)・共有画像画面(S5)・ホーム(S1)・しおり作成(S2)は旧トークンのまま。デザイン案の対象外
  - 写真の実データ(実際のJPEG)での表示確認はしていない(単色PNGで代用)
- 不明点・仮置き:
  - 共有導線はヘッダーのアイコンと記録タブ下部のカードの2箇所になった。片方に寄せるかは要判断
  - 広告枠(F9)を破線カードに変えたが、AdSense実装時の見た目は別途調整が必要
- 成果物: `src/components/log/`(9ファイル)、`src/components/common/AdSlotPlaceholder.tsx`、`docs/design/screens/S3d_ログ.md`
