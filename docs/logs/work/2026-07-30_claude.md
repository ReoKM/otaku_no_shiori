# 2026-07-30 claude

## Supabase未連携による影響の調査

### やったこと

- リポジトリ全体でSupabase参照箇所を洗い出し(`package.json` / `src/` / `netlify/` / `supabase/`)
- `supabase/README.md`・`docs/03_tech_stack.md`・`docs/02_roadmap.md`・`docs/plans/2026-W29.md`・`docs/plans/2026-W30.md` を照合
- OPEN Issue 24件・OPEN PR 5件を確認し、ブロック中タスクと未消化チェック項目を特定
- 影響を7点+「影響が出ていない範囲」に整理して報告

### 判明した現在地

| 項目 | 状態 |
|---|---|
| Supabaseプロジェクト作成 | 完了(2026-07-11、PR #32) |
| マイグレーション0001の適用 | 未(Issue #72) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` 登録 | 未(Issue #72・PR #32) |
| X/Google OAuthプロバイダ設定 | 未(Issue #72) |
| アプリ側のSupabaseクライアント | 未実装(`@supabase/supabase-js` 依存なし・`src/lib/supabase/` なし) |
| `.env.example` | mainに未マージ(PR #32 がdraftのまま) |

### 特定した影響

1. F8(ログイン+ゲストデータ移行)が未着手。`docs/plans/2026-W30.md` タスク#5が「ブロック中」のまま2週間停止し、W3マイルストーン=Phase 1公開条件が未達
2. データが端末内のみ。`src/app/privacy/page.tsx:43`・`src/app/terms/page.tsx:50` で消失の免責を書くしかなく、しおりの資産化という価値提案と衝突
3. `src/components/share/LoginHint.tsx` が遷移先のない「ログインすると〜」を表示。S6(設定/アカウント)画面が未実装で画面一覧に欠番
4. 写真がStorageに載らず、共有画像生成でdata URLとしてNetlify Functionへ送る構造。`docs/03_tech_stack.md` の「写真はStorage側に載せてNetlify帯域を抑える」方針と逆。ペイロード上限超過の不具合が実際に発生(コミット 25d32a8 で修正済み)。`photos` バケットの2MB上限・MIME制限による多層防御も未発動
5. RLSが一度も実環境で検証されていない。SQL自体の実行可否も未確認。Issue #13 の実環境確認が7/9からOPENのまま
6. 適用待ちの間にスキーマが先行。PR #85 が `budget_items`(DB_VERSION 3)を追加済みだが `supabase/migrations/` は0001のみ。適用時に0002以降が別途必要
7. スポットDBはMVPではシードJSON同梱(W29オーナー判断1-A)で回避済み。ただしPhase 2のUGC公開申請フローは `spots` テーブル前提で、シード更新のたびに本番デプロイ(15クレジット/回)が必要

影響が出ていない範囲: F1〜F7・F9・F10はゲスト保存で完結する設計を維持しており機能は動作する(W29オーナー判断2-Aによる意図した順序)。

### 完了報告

- Goal: Supabase連携の停滞が、いま何をブロックし何を壊しているかを、コード・docs・Issue/PRの実態から特定して報告する
- 結果: 達成
- できていないこと:
  - Supabase管理画面・Netlify管理画面の実物は確認していない(オーナー権限が必要)。状態判定はリポジトリ内のdocs・Issue・PRの記述に基づく
  - 影響の解消(F8実装・マイグレーション適用)は本タスクのスコープ外
- 不明点・仮置き:
  - `docs/plans/2026-W29.md` は「Supabase接続情報(URL/anonキー)はNetlify環境変数に登録済み」と記述する一方、PR #32・Issue #72 は未登録扱い。どちらが実態かオーナー確認が必要。本報告は「未登録」側(より新しいIssue #72)を前提とした
- 成果物: 本作業ログ(調査報告のみ。コード変更なし)
- 作業ログ: docs/logs/work/2026-07-30_claude.md
